{
  "name": "ph646main-website",
  "version": "1.0.0",
  "description": "PH646 Philippines - Online Casino & Sports Betting (ph646main.com)",
  "scripts": {
    "dev": "npx --yes serve .",
    "deploy": "npx wrangler pages deploy . --project-name=ph646main",
    "deploy:pages": "npx wrangler pages deploy . --project-name=ph646main",
    "convert-visuals": "node scripts/convert-visuals-to-webp.js",
    "build:blogs-json": "node scripts/build-blogs-json-from-news.js",
    "rebuild:news-articles": "node scripts/rebuild-news-articles.js",
    "sync": "node scripts/content-sync.js",
    "sync:all": "node scripts/content-sync.js --all",
    "sync:refresh": "node scripts/content-sync.js --refresh",
    "sync:force": "node scripts/content-sync.js --force",
    "sync:daily": "node scripts/content-sync.js --daily",
    "sync:doctor": "node scripts/sync-doctor.js",
    "backfill": "node scripts/backfill-internal-links.js",
    "backfill:force": "node scripts/backfill-internal-links.js --force",
    "audit:links": "node scripts/audit-internal-links.js",
    "resort:blogs": "node -e \"const {sortBlogsForIndex}=require('./scripts/lib/sort-blogs.js');const fs=require('fs');const p='assets/data/blogs.json';const b=JSON.parse(fs.readFileSync(p,'utf8'));fs.writeFileSync(p,JSON.stringify(sortBlogsForIndex(b),null,2)+'\\n');console.log('Resorted',b.length,'posts');\""
  },
  "keywords": [
    "PH646",
    "ph646main",
    "casino",
    "sports-betting",
    "philippines"
  ],
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "dotenv": "^16.4.7",
    "sharp": "^0.34.5"
  }
}
